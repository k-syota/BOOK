<h2>User</h2>

  <table>
  	<thead>
    <tr>
    <th>image</th>
  	<th>name</th>
    </tr>
    </thead>
    <tbody>
    <tr>
    <% @user.each do |user| %>

      <td>
        <%= user.name %>
      </td>
      <td>
        <%= link_to "Show", user_path(user.id) %>
      </td>
    </tr>
    <% end %>
    </tbody>
  </table>

